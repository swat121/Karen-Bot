microcontroller:
  relayS:
    1: "On"
    0: "Off"
  lightS:
    1: "On"
    0: "Off"
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_HOST:http://localhost:8086/eureka/}
  instance:
    instance-id: karen-bot-instance
spring:
  application:
    name: karen-bot